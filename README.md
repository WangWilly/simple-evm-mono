# Simple EVM Mono

## Lerna build and run

```bash
# Install dependencies
npm install

# Build the project
npx lerna run build

# Run the project
npx lerna run start
npx lerna run start --scope {backend|contracts}
```
